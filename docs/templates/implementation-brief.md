# Implementation brief template

- Task ID and title:
- Assigned implementer (Claude/Grok/other):
- Base commit and dedicated branch/worktree:
- Goal, in player-visible terms:
- Read first (exact authoritative documents):
- Own these paths:
- Shared interfaces that may be consumed; changes requiring integration:
- Input asset revision, manifest and admission status:
- Required behavior, including one concrete example:
- Failure/recovery cases:
- Explicit exclusions:
- Acceptance checks and evidence paths:
- Return: changed files, assumptions, validation results, commit SHA, publication state and remaining gaps.

Do not redesign the art direction, regenerate admitted sprites or expand the gameplay system beyond this task. If a missing contract prevents implementation, identify the smallest decision needed and continue independent work. Placeholder visuals must be labeled as placeholders.
